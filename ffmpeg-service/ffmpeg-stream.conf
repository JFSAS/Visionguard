# FFmpeg流配置文件

# 视频文件数组
VIDEO_FILES=(
    "/opt/ffmpeg-stream/videos/1.mp4"
    "/opt/ffmpeg-stream/videos/2.mp4"
    # 添加更多视频文件
)

# RTMP URL数组（与视频文件一一对应）
RTMP_URLS=(
    "rtmp://localhost:1935/rawdata/1"
    "rtmp://localhost:1935/rawdata/2"
    # 添加更多RTMP URL
)

# 可选：FFmpeg额外参数
# FFMPEG_EXTRA_ARGS="-loglevel quiet"